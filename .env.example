# LLM Provider API Keys (choose one)
# Browser-Use is open-source - you can use any supported LLM provider

# Google Gemini (Free tier available - recommended for open source)
# Get your free key at: https://aistudio.google.com/app/u/1/apikey?pli=1
GOOGLE_API_KEY=your_google_api_key_here

# OpenAI (alternative option)
# OPENAI_API_KEY=your_openai_api_key_here

# Anthropic Claude (alternative option)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Browser Mode: Set to true to use existing Chrome browser (avoids permission issues)
USE_CDP=true


# Async monitor: LLM-driven gameplay (read game code → generate play instructions → LLM decides each action)
# Set to true for unified LLM gameplay+detection; false for hardcoded player + detector loops
USE_LLM_GAMEPLAY=true

# Path to game source for play-instruction generation (used when USE_LLM_GAMEPLAY=true)
# GAME_SOURCE_PATH=template-youtube-playables/src
